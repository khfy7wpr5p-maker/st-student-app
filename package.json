{
  "name": "st-student-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:browser": "node --test browser-tests/*.spec.mjs"
  },
  "devDependencies": {
    "@xmldom/xmldom": "0.9.12",
    "fake-indexeddb": "6.2.5",
    "playwright": "1.62.1"
  }
}
